<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for a Compose app: no action bar, Compose draws everything. -->
    <style name="Theme.FlashMD" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/ic_launcher_background</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
